# Placeholders a completar

Listado de elementos placeholder que debés reemplazar con tu información real antes del deploy.

---

## Formulario de contacto
- **Componente:** `src/components/Contact.jsx` — conectar el `handleSubmit` con un servicio de email (Formspree, EmailJS, Resend, etc.)

## Badge de LinkedIn
- **Componente:** `src/components/Contact.jsx` — agregar el widget embebido de LinkedIn donde dice "PLACEHOLDER: Agregar badge de LinkedIn"

## Certificaciones
- **Componente:** `src/components/Education.jsx` — agregar certificaciones al array `timeline` siguiendo el formato comentado en el código

## Favicon
- **Archivo:** Reemplazar `public/vite.svg` con un favicon personalizado

## Deploy
- Para **GitHub Pages**: ya está configurado `base: './'` en `vite.config.js`
- Para **Vercel**: no se necesita configuración adicional, hacer deploy directamente
